To start a new project:

```
$ mkdir my-project
$ git clone https://github.com/danibezoff/webpack-4-boilerplate.git my-project
$ cd my-project
$ rm -rf .git README.md
$ git init
$ npm install
$ git add -A
$ git commit -m 'Initial commit'
```
